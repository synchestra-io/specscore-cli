# Feature: Example

Fixture parent-Feature README so the issue's `affected_component`
reference resolves. Other lint rules may flag this file as incomplete;
the I-012 test filters by rule ID so unrelated violations are ignored.

# Feature: Example

Fixture parent-Feature README so the Feature-scoped issue passes I-009's
parent-existence check. Other lint rules may flag this file as
incomplete; the I-011 test filters by rule ID so unrelated violations
are ignored.
